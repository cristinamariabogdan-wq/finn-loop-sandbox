{
  "name": "finn-loop-sandbox",
  "version": "0.1.0",
  "description": "Sandbox for testing Finn-loop",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
